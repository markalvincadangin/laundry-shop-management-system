<!-- harness:begin -->
## Research Summary (Research Harness)
Generated by /speckit-harness-report on 2026-07-14. State: specs/010-config-standardization/harness/

### Verdict
The investigation into the seeding strategy confirms that `DemoDataSeeder.java` is currently hardcoded with `@Profile("dev")` but duplicates logic intended for Flyway migrations [E001]. Furthermore, the `V2__seed_users.sql` migration referenced in the Constitution does not exist in the codebase [E002]. The recommended and standard approach is to extract the seed logic into a Flyway environment-specific migration (e.g., `db/migration/dev/V2__seed_demo_data.sql`), effectively replacing and deleting `DemoDataSeeder.java`. 

### Requirement Coverage — 0/8 verified
| Requirement | Status | Evidence | Verification |
|-------------|--------|----------|--------------|
| FR-001 .env.example definitions | uncovered | | |
| FR-002 .gitignore protection | uncovered | | |
| FR-003 docker-compose standards | uncovered | | |
| FR-004 @ConfigurationProperties | uncovered | | |
| FR-005 Modern Security DSL | uncovered | | |
| FR-006 Filter ordering | uncovered | | |
| FR-007 DemoDataSeeder gating | covered-unverified | E001, E002 | none |
| FR-008 Makefile targets | uncovered | | |

### Key Findings (curated set)
- **[high]** DemoDataSeeder uses `@Profile("dev")` but duplicates Flyway user seeding. Industry standard is Flyway environment-specific migrations (e.g. `R__demo_data.sql`) or separating to `src/test`. (Source: `DemoDataSeeder.java`, Status: unverified)
- **[high]** `V2__seed_users.sql` is mentioned in docs but does not exist in `src/main/resources/db/migration`. `DemoDataSeeder.java` was likely created to replace it without updating the docs. (Source: `db/migration`, Status: unverified)

### Corrected Assumptions
- *Assumption*: `V2__seed_users.sql` handles user seeding. *Correction*: The file does not exist; `DemoDataSeeder.java` currently handles all seed data. (E001, E002)

### Open Questions
- FR-001 through FR-006, and FR-008 are currently uncovered by the harness as they are standard refactoring tasks that will be explored during the implementation planning phase.
- **Spec Correction Needed**: FR-007 currently specifies gating `DemoDataSeeder.java`, but it should be updated to require deleting it and replacing it with a Flyway migration.

### Method & Budget
Actions spent: 1 search, 1 inspection. Stop reason: Mission answered. (See [budget.md](file:///home/markc/projects/web-dev/laundry-shop-management-system/specs/010-config-standardization/harness/budget.md))
<!-- harness:end -->
