/**
 * Shared Playwright fixtures and re-exports for E2E tests.
 *
 * All spec files should import { test, expect } from './fixtures'
 * to ensure consistent configuration across the suite.
 */
export { test, expect } from '@playwright/test';
