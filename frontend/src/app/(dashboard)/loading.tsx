"use client";

import { LoadingState } from "@/features/shared";

/**
 * Root Dashboard Loading Boundary
 * Standardized loading state for the Faith Laundry Command Center.
 */
export default function Loading() {
  return <LoadingState />;
}
