import React from "react";

export default function Loading() {
  return (
    <div className="w-full h-[60vh] flex flex-col items-center justify-center gap-4 animate-in fade-in duration-500">
      <div className="relative">
        <div className="h-12 w-12 rounded-full border-2 border-action/20 border-t-action animate-spin" />
        <div className="absolute inset-0 bg-action/5 rounded-full blur-xl animate-pulse" />
      </div>
      <span className="text-[10px] font-bold uppercase tracking-[0.2em] text-action/60 animate-pulse">
        Initializing System
      </span>
    </div>
  );
}
