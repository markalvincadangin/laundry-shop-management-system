# Frontend Environment Template — Faith Laundry Shop Management System
# Copy to frontend/.env.local and fill in values. NEVER commit .env.local to Git.
# Next.js loads .env.local automatically when running from frontend/ directory.

# API Configuration (backend base URL)
NEXT_PUBLIC_API_URL=http://localhost:8080/api
