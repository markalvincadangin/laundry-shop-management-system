# Host dev (recommended): run backend via Docker, frontend natively with Turbopack
#   docker compose up db backend
#   cd frontend && npm run dev
# Use the standard BACKEND_PORT
NEXT_PUBLIC_API_URL=http://localhost:8080/api
